version: '3.8'

services:
  lebdata-web:
    build: .
    ports:
      - "5050:5000"
    volumes:
      - ./workspace:/app/workspace
      - ./lebdata:/app/lebdata
      - ./scripts:/app/scripts
    environment:
      - FLASK_ENV=development
      - FLASK_DEBUG=1
